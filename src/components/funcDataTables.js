import React from "react";

function AdminDataTableArray (element, url, typeId) {
    var columns = [];
    columns.push(
        [
            { 'data': 'num' },
            { 'data': 'name' },
            { 'data': 'docNumberPrefixes.name' },
            { 'data': 'finalDoc' }
        ]
    );

    // return (
    // )
}

